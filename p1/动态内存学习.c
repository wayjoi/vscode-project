/*在内存堆中创建空间
#include<stdlib.h>
 p=(int)*malloc(unsigned int size);//开辟字节数字大小,强制类型转换
 p=*calloc(unsigned n,unsigned size);//元素个数+长度
 realloc(void *p,unsigned int size);//重新分配内存大小
 free(p);
 void *p3//无类型指针变量，适用于数据转换
*/